[package]
name = "silence-strip-langs"
version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
repository = { workspace = true }
description = "Language/grammar registry for `silence` — TypeScript/JavaScript, Python built-in; Rust, Go, C/C++, TOML on demand."
keywords = ["tree-sitter", "comments", "parser", "grammar"]
categories = ["parser-implementations", "development-tools"]

[lib]
name = "silence_langs"
path = "src/lib.rs"

[lints]
workspace = true
